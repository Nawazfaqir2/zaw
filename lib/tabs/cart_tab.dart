import 'package:flutter/material.dart';

class cart_tab extends StatelessWidget {
  const cart_tab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}